import React from "react";

/**
 * A wrapper for a group of detail rows.
 */
const DetailInfoGroup = ({ children }) => {
  return <div style={{ borderBottom: "1px solid #1a1a1a" }}>{children}</div>;
};

export default DetailInfoGroup;
